<h1><%= @album.title %></h1>

<%= simple_form_for [@album, @post] do |f| %>
  <%= f.input :description, input_html: { class: "form-control" } %>
  <%= f.file_field :images, multiple: true, name: "images[]" %> 
  <%= f.submit "Submit", class: "btn btn-primary", data: {disable_with: "Submitting..."} %>
<% end %>
